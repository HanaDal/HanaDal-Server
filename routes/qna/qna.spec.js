const supertest = require('supertest');
const app = require('../../index');

describe('GET /', () => {
    describe('Success', () => {
        it('', done => {

        });
    });

    describe('Failure', () => {
        it('', done => {
            
        });
    });
});